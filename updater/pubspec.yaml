name: vocal_updater
description: "VOCAL-APP updater — sürüm kontrolü yapar, güncelleme indirir, uygulamayı başlatır."
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.11.5

dependencies:
  http: ^1.2.2
  package_info_plus: ^8.0.0

dev_dependencies:
  lints: ^6.0.0
